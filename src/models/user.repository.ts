import { User } from "./user.model";

const TABLE = 'Users';

const getAll = () => {};

const getById = (_id: string) => {};

const save = (_newUserParams: User) => {};

const update = (_id: string, _userUpdates: User) => {};

const remove = (_id: string) => {};

export default { getAll, getById, save, update, remove };